# CallNexus 功能路线图

本文档用于统一记录 CallNexus 呼叫中心系统的功能模块、开发顺序和当前进度。后续 AI 或 Claude Code 接手开发时，先阅读本文档，再结合 `CALL_CENTER_ENGINEERING_GUIDE.md` 和 `AI_DEVELOPMENT_GUIDE.md` 执行具体实现。

## 状态说明

- `[DONE]`：基础功能已实现，可以进入联调或后续增强。
- `[PARTIAL]`：已有代码或页面，但能力未闭环，仍需要继续开发。
- `[PLANNED]`：规划中，尚未正式实现。
- `[BLOCKED]`：代码侧已有准备，但依赖服务器、FreeSWITCH 或外部配置。

## 当前进度

当前处于 **阶段 3：网关、号码和呼入路由基础能力开发中**：

- 阶段 1：呼叫中心基础底座已经基本完成，包括 FreeSWITCH 节点、SIP 账号、坐席、坐席工作台、ESL 控制、客户、工单、动态表单模板。
- 阶段 2：FreeSWITCH 动态 Directory 已完成联调，后台新增 SIP 账号后，FreeSWITCH 可以通过 CallNexus 动态获取分机目录并完成注册。
- 阶段 3：网关管理第一版和动态 Gateway XML 后端输出已完成，下一步开发号码管理，再进入动态 Dialplan 和呼入路由。

当前最优先事项：

1. 开发号码管理。
2. 开发动态 Dialplan 和呼入路由。
3. 补齐通话记录、录音、事件持久化。
4. 再进入 IVR、技能组、队列、外呼和 AI 辅助。

## 功能模块总览

| 模块 | 状态 | 当前说明 |
| --- | --- | --- |
| 基础平台登录 | `[DONE]` | 登录后台已可用，沿用现有系统能力。 |
| 首页运营概览 | `[DONE]` | 已从介绍页调整为呼叫中心运营首页。 |
| 坐席电话条 | `[PARTIAL]` | 已支持签入、签出、示忙、示闲、在线拨打和基础通话状态展示；软电话实际媒体通话已转向“普通 SIP 软电话 + ESL 控制”方案。 |
| FreeSWITCH 节点管理 | `[DONE]` | 已支持节点配置，包括 SIP 域、WSS 地址、ESL 地址、ESL 端口、ESL 密码等。 |
| SIP 账号管理 | `[DONE]` | 已支持后台账号管理、密码加密、坐席绑定，并已通过 FreeSWITCH 动态 Directory 完成注册联调。 |
| 坐席管理 | `[DONE]` | 已支持坐席与系统用户、SIP 账号、FreeSWITCH 节点关联。 |
| ESL 连接与监听 | `[PARTIAL]` | 已有 ESL 连接、命令执行、事件分发、事件处理器；仍缺事件落库、异常恢复策略增强、集群调度。 |
| 呼叫控制 | `[PARTIAL]` | 已支持通过 ESL 发起呼叫、挂断、清理活动通话；仍缺保持、转接、三方、监听、强插等。 |
| 实时状态推送 | `[PARTIAL]` | 已支持部分通话和坐席状态通过前端展示；仍需统一事件模型和断线重放策略。 |
| FreeSWITCH XML Directory | `[DONE]` | 后端接口和服务器侧 `mod_xml_curl` 联调已完成，后台新增 SIP 账号可以动态注册。 |
| FreeSWITCH XML Dialplan | `[PLANNED]` | 尚未实现，后续用于动态呼入路由、IVR、外呼路由。 |
| FreeSWITCH XML ACL | `[PLANNED]` | 尚未实现，后续用于集中维护可信 IP、网关来源等。 |
| 网关管理 | `[PARTIAL]` | 已完成后台网关 CRUD、前端页面、菜单权限、数据表和动态 Gateway XML 后端输出；仍需服务器侧 FreeSWITCH 配置联调。 |
| 号码管理 | `[PLANNED]` | 后续管理 DID、主叫号码、号码归属租户、呼入绑定路由。 |
| 客户管理 | `[PARTIAL]` | 已支持客户创建、号码重复时带出已有客户、详情展示、自定义字段展示。 |
| 客户动态字段 | `[PARTIAL]` | 已支持输入框、文本框、日期、单选、多选、下拉等字段类型，以及标签和值分离。 |
| 客户跟进记录 | `[PARTIAL]` | 已支持在客户详情右侧 Tabs 展示跟进记录；后续可扩展通话记录、工单记录、短信记录。 |
| 工单管理 | `[PARTIAL]` | 已支持工单创建、详情、自定义字段展示；后续需要状态流转、处理记录、SLA。 |
| 动态表单模板 | `[PARTIAL]` | 已支持客户和工单模板、字段选项、字段布局；后续需要版本管理、复制模板、字段校验规则增强。 |
| 通话记录 CDR | `[PLANNED]` | 尚未完整实现，需要从 ESL 事件和 FreeSWITCH CDR 生成统一通话记录。 |
| 录音管理 | `[PLANNED]` | 尚未实现，需要关联通话记录、坐席、客户、工单。 |
| IVR 管理 | `[PLANNED]` | 代码目录已有基础模块，但前端尚未挂完整菜单和闭环能力。 |
| 技能组管理 | `[PLANNED]` | 后续用于排队、路由、坐席分配。 |
| 队列管理 | `[PLANNED]` | 后续用于呼入排队、等待音、溢出策略、超时策略。 |
| 自动外呼任务 | `[PLANNED]` | 后续分预览式、渐进式、预测式、机器人外呼。 |
| 预览式外呼 | `[PLANNED]` | 坐席先看到客户资料，手动点击拨打。 |
| 渐进式外呼 | `[PLANNED]` | 系统按空闲坐席自动拨号，接通后分配给坐席。 |
| 预测式外呼 | `[PLANNED]` | 系统根据接通率和坐席空闲率预测拨号量。 |
| 机器人外呼 | `[PLANNED]` | AI 或语音机器人自动完成外呼，复杂度最高，放后期。 |
| 知识库 | `[PLANNED]` | 后续用于人工接听时实时推荐话术和答案。 |
| AI 坐席辅助 | `[PLANNED]` | 后续支持实时转写、对话流、话术推荐、情绪识别、摘要。 |
| AI 自动建单 | `[PLANNED]` | 后续基于对话内容抽取客户信息、工单字段，先生成草稿，由人工确认。 |
| 运营报表 | `[PLANNED]` | 后续统计呼入量、接通率、坐席效率、排队时长、满意度等。 |
| 监控告警 | `[PLANNED]` | 后续监控 FreeSWITCH、ESL、WebSocket、队列堆积、呼损率。 |

## 推荐开发顺序

### 阶段 0：基础平台

状态：`[DONE]`

目标：保证系统可以登录、授权、菜单展示、基础租户体系可用。

已完成内容：

- 后台登录。
- 系统菜单基础能力。
- 首页从介绍页调整为呼叫中心运营首页。

### 阶段 1：呼叫中心基础底座

状态：`[PARTIAL]`

目标：先让坐席、SIP 分机、FreeSWITCH 节点、ESL 控制和基础客户工单跑通。

已完成内容：

- FreeSWITCH 节点管理。
- SIP 账号管理。
- 坐席管理。
- 坐席电话条。
- ESL 命令控制。
- ESL 事件监听基础结构。
- 客户管理。
- 工单管理。
- 动态表单模板。
- 客户跟进记录。

仍需补齐：

- ESL 事件落库。
- 通话生命周期状态强一致处理。
- 软电话挂断后浏览器端状态自动同步的更多边界场景。
- 呼叫保持、转接、咨询、三方通话等坐席高级操作。

### 阶段 2：FreeSWITCH 动态配置

状态：`[PARTIAL]`

目标：后台新增或修改 SIP 账号、网关、路由后，FreeSWITCH 不需要手工改 XML 文件。

当前已完成：

- 后端动态 Directory XML 接口。
- Directory 安全 Token 配置。
- SIP 账号查询和 XML 渲染结构。
- 服务器侧 `mod_xml_curl` 配置和联调。
- 后台新增 SIP 账号后，普通 SIP 软电话可以注册成功。

后续需要开发：

- 动态 Dialplan XML。
- 动态 Gateway XML 后端输出已完成，等待服务器侧 FreeSWITCH 配置联调。
- 动态 ACL XML。
- XML 请求日志和调试页面。

### 阶段 3：网关、号码和呼入路由

状态：`[PARTIAL]`

目标：让系统可以接运营商线路，支持外线呼入。

开发内容：

- 网关管理：SIP Trunk 地址、端口、认证、方向、状态。第一版和动态 Gateway XML 后端输出已完成，尚未完成服务器侧联调。
- 号码管理：DID、主叫号码、归属租户、启停状态。
- 呼入路由：号码绑定 IVR、队列、坐席、技能组。
- 呼出路由：按租户、坐席、号码池选择出口网关。

### 阶段 4：通话记录和录音

状态：`[PLANNED]`

目标：每通电话都能追踪、查询、回放和关联业务。

开发内容：

- CDR 通话记录表。
- ESL 事件转通话生命周期。
- 录音文件路径、时长、存储状态。
- 客户详情和工单详情增加通话记录 Tab。
- 呼叫失败原因归类。

### 阶段 5：IVR

状态：`[PLANNED]`

目标：支持客户来电后按语音菜单自动分流。

开发内容：

- IVR 流程管理。
- IVR 节点管理。
- 语音文件管理。
- DTMF 按键路由。
- IVR 执行记录。
- IVR 版本发布。

### 阶段 6：技能组和队列

状态：`[PLANNED]`

目标：支持标准呼叫中心的排队和分配。

开发内容：

- 技能组管理。
- 坐席技能绑定。
- 队列管理。
- 队列策略：最长空闲、轮询、随机、优先级。
- 等待音、超时、溢出、无人接听处理。
- 坐席状态和队列状态实时看板。

### 阶段 7：外呼任务

状态：`[PLANNED]`

目标：支持人工和自动化外呼。

建议顺序：

1. 预览式外呼：坐席确认客户后手动拨打。
2. 渐进式外呼：系统按空闲坐席自动发起呼叫。
3. 预测式外呼：系统根据接通率和坐席空闲率预测拨号。
4. 机器人外呼：AI 或语音机器人自动完成外呼。

开发内容：

- 外呼任务。
- 外呼名单。
- 外呼批次。
- 号码去重和黑名单。
- 外呼结果。
- 失败重试。
- 外呼统计。

### 阶段 8：AI 和知识库

状态：`[PLANNED]`

目标：让人工接听时获得实时辅助，而不是一开始就替代人工。

第一阶段先做人工辅助：

- 实时语音转写。
- 前端展示双方对话流。
- 根据知识库推荐话术。
- 自动识别客户意图。
- 自动提取客户字段。
- 自动生成工单草稿。
- 自动生成通话摘要。

第二阶段再做半自动处理：

- AI 推荐客户分类。
- AI 推荐工单类型和优先级。
- AI 推荐下一步跟进动作。
- 人工确认后保存。

第三阶段再考虑机器人：

- 机器人外呼。
- 机器人 IVR。
- 人机协同转人工。

### 阶段 9：报表和监控

状态：`[PLANNED]`

目标：支撑运营管理和系统稳定性。

开发内容：

- 呼入量、呼出量、接通率。
- 坐席在线时长、通话时长、空闲时长、示忙时长。
- 队列等待时长、放弃量、呼损率。
- 网关状态、ESL 状态、FreeSWITCH 状态。
- 告警中心。
- 导出报表。

## 当前下一步建议

下一步不要直接做 AI、机器人或预测外呼，当前基础链路还没完全闭环。

建议立刻处理：

1. 开发号码管理。
2. 开发动态 Dialplan 和呼入路由。
3. 配置并联调 FreeSWITCH Gateway XML 输出。
4. 开发 CDR 通话记录。

完成以上内容后，再进入 IVR 和队列会更稳，因为 IVR、队列、外呼和 AI 都依赖稳定的号码、路由、通话记录和事件模型。
